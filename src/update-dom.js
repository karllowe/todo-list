import { defaultProject } from "./project-functions";


const updateDom = (() => {
    function rebuildDom() {
        const items = defualtProject;
    }

    return {rebuildDom}
})();

export {updateDom}